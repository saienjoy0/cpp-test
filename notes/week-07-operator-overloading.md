# 2026 程序设计 II - 第7周课后选择题

## 主题
运算符重载、成员函数重载、前缀/后缀自增、深拷贝赋值

## Questions

共20题（用户提供题目）。

主要知识点：
- 运算符重载目的
- `operator+` 等重载函数命名
- 二元运算符成员函数参数规则
- 不可重载运算符：`::`、`.`、`.*`、`?:`
- `operator->`
- 前缀 `++` 返回 `T&`
- 后缀 `++` 返回副本
- `+=` 常见实现方式
- 指针成员的浅拷贝风险
- 深拷贝策略

选择题进度：20 / 20
